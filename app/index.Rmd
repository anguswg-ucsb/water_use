---
title: "US water withdrawals"
runtime: shiny
output: 
  flexdashboard::flex_dashboard:
    navbar:
      - { icon: "fa-question-circle", href: "https://github.com/anguswg-ucsb/water_use", align: right }
    theme: cosmo
    orientation: rows
    source_code: embed
    vertical_layout: fill
---


```{r setup, include = FALSE}
library(shiny)
library(flexdashboard)
library(shinyWidgets)

library(dplyr)
library(tidyr)
library(janitor)
library(stringr)
library(dataRetrieval)

library(DT)
library(highcharter)
library(RColorBrewer)
source('utils.R')
```


```{r context="server"}
#Initialize Maps 
# output$conusMap     <- renderLeaflet({ basemap() })
state_lst <- data.frame(state = state.name)
county_lst <- USAboundaries::us_counties() %>% 
  sf::st_drop_geometry()

water_use <<- reactive({
    df <- dataRetrieval::readNWISuse(stateCd= state()$state_name[1], countyCd = county()) %>%
            clean_names()
})

# output$countyMap <- renderLeaflet({ pop_map(pop) })

```

NWIS water use data {data-icon="fa-tint"}
=====================================

Inputs {.sidebar}
-------------------------------------

### Model Filters
```{r}
# dateRangeInput("dateTS", label = "Date Range Input",
#                start = "1993-01-01",
#                end = "2018-12-31",
#                min = "1993-01-01",
#                max = "2018-12-31",
#                format = "yyyy mm dd",
#                weekstart = 1)

# state, county, usage and sector UI outputs
uiOutput("stateSelect")
uiOutput("countySelect")
# uiOutput("usageSelect")
uiOutput("sectorSelect")


```

```{r context="server"}
# sector_lst <- data.frame(source = c("consumptive", "withdrawals", "public",
#                                     "domestic","commericial","industrial", "mining",
#                                     "irrigation_total","irrigation_crop", "irrigation_golf",
#                                     "livestock", "aquaculture", "total_thermoelectric",
#                                     "fossil_fuel", "nuclear", "hydro",
#                                     "geothermal", "fossil_fuel", "wastewater"))
sector_lst <- data.frame(source = c("_total_self_supplied_withdrawals_fresh_", "surface_water_withdrawals_fresh", "groundwater_withdrawals_fresh"))

# usage <- data.frame(source = c("consumptive", "withdrawals"))

# State input 
output$stateSelect <- renderUI({
   selectInput("stateSearch", "Choose state:",
               as.list(unique(county_lst$state_name)),
               multiple= FALSE,
               selectize = TRUE)
})

# County input
output$countySelect <- renderUI({
   selectInput("countySearch", "Choose county:",
               choices = NULL,
               multiple= FALSE,
               selectize = TRUE)
})
# Usage input
# output$usageSelect <- renderUI({
#    selectInput("usageSearch", "Sink:",
#                choices = c("consumptive", "withdrawals"),
#                multiple= FALSE,
#                selectize = TRUE)
# })
# Sector input
output$sectorSelect <- renderUI({
   selectInput("sectorSearch", "Choose sector:",
               choices = as.list(sector_lst),
               multiple= FALSE,
               selectize = TRUE)
})

```

### 
```{r}
shiny::actionButton("submitButton", label = "Enter", icon("search"))
```


```{r context="server"}
# Make state input a reactive value
state <- reactive({
  if(is.null(input$stateSearch)){
    NULL
  } else {
    filter(county_lst, state_name == input$stateSearch)
  }
  })
# output$stateText <- renderPrint({
#   state()
#   })
# usage <- reactive({
#   input$usageSearch
# })
# output$usageText <- renderPrint({
#   usage()
#   })
# When state input changes, county choices update to respective state
observeEvent(state(), {
  choices <- state()$name
  updateSelectInput(session, inputId = "countySearch", choices = choices)

})

# Make county input a reactive value
county <- reactive({
  input$countySearch
})

# Reactivity test for county input
output$countyText <- renderPrint({
   county()
  })

# Make sector input a reactive value
sector <- reactive({
  input$sectorSearch
})
```



Row {.tabset}
----------------------------------------------
### Water withdrawals
```{r}
highchartOutput("waterUseGraph")
```

```{r context = "server"}
# county water use graph, reactive to state, county, and sector inputs
waterUseData <- eventReactive(input$submitButton, {
     df <- tidy_sectors(water_use())
     # cols <- viridisLite::viridis(12)
     # cols <- substr(cols, 0, 7)
     
      if(!is.null(input$sectorSearch)) {
          df <- df %>% filter(str_detect(sector, sector()))
          num = length(unique(df$sector))
          cols = colorRampPalette(brewer.pal(6, "Paired"))(num)
          highchart() %>%
              hc_add_series(df, type = "column", hcaes(x = year, y = withdrawals, group = "sector")) %>%
              hc_colors(cols) %>% 
              hc_add_theme(hc_thm = hc_theme_smpl())
      } else {
        NULL
      }
})

# Render water use plot after action button is clicked
output$waterUseGraph <- renderHighchart({
        waterUseData()
      })
```

### Population
```{r}
highchartOutput("popGraph")
```
 
```{r context = "server"}
popData <- eventReactive(input$submitButton, {
         df <- dataRetrieval::readNWISuse(stateCd= state()$state_name[1], countyCd = "ALL") %>%
            clean_names()
     
      if(!is.null(input$sectorSearch)) {
          df <- get_pop_data(df)
          num = length(unique(df$county_nm))
          cols = colorRampPalette(brewer.pal(6, "Paired"))(num)
          
          highchart() %>%
              hc_add_series(df, type = "column", hcaes(x = year, y = growth_rate, group = "county_nm")) %>%
              hc_colors(cols) %>% 
              hc_add_theme(hc_thm = hc_theme_smpl())
      } else {
        NULL
      }
})

output$popGraph <- renderHighchart({
        popData()
      })
```


### Data table 
```{r}
DTOutput("waterDT")
```

``` {r context = "server"}
# county water use data table, reactive to state, county, and sector inputs
waterData <- eventReactive(input$submitButton, {
     df <- tidy_sectors(water_use())
     if(!is.null(input$sectorSearch)) {
       df <- df %>% filter(str_detect(sector, sector()))
       DT::datatable(df, fillContainer = TRUE)
    } else {
        NULL
      }
}) 
     # else if(!is.null(input$usageSearch) && !is.null(input$sectorSearch)) {
     #   df <- df %>%
     #     filter(str_detect(sector, usage())) %>%
     #      filter(str_detect(sector, sector()))
     #   DT::datatable(df, fillContainer = TRUE)
     # }
# Render water use data table after action button is clicked
output$waterDT <- renderDT({
  waterData()
})

```





